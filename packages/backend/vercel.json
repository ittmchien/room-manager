{
  "version": 2,
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm build:shared && cd packages/backend && npx prisma generate && npx nest build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
